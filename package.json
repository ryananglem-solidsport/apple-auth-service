{
  "name": "apple-auth-service",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "node server.js",
    "build": "rm -r dist && mkdir dist && babel ./netlify/functions/auth.js -s -d dist"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "apple-signin-auth": "^1.7.5",
    "express": "^4.18.2"
  },
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "netlify-cli": "^12.10.0"

  }
}
